{
  "name": "cleave-sdk",
  "version": "4.0.0",
  "description": "Infinite context for Claude Code — chain sessions together automatically with self-authored handoffs, knowledge accumulation, and enforced handoff hooks.",
  "main": "dist/index.js",
  "bin": {
    "cleave": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "claude",
    "agent",
    "relay",
    "session",
    "context",
    "handoff",
    "infinite",
    "claude-code"
  ],
  "author": "Israel",
  "license": "MIT",
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.59",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
